version: '3'

services:
  api:
    build:
      context: ../.
      dockerfile: docker/Dockerfile.prod
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ../:/app
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.uacl.rule=Host(`api.emotives.net`)"
      - "traefik.http.routers.uacl.tls=true"
      - "traefik.http.routers.uacl.tls.certresolver=letsencrypt"
    networks:
      traefik:

networks:
  traefik:
    external:
      name: traefik